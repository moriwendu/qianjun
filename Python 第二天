
字符串中的下标

    name = "abcdefg"
    name[0] 是 a 
    name[1] 是 b
    ...
    name[6] 是 g

    name[-1] 是 g        与 name[len(name)-1] 等同
    name[-2] 是 f
    ...
    
    下标从左向右取值是从 0 开始
    下标从右向左取值是从-1 开始
